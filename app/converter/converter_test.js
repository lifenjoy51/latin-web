'use strict';

describe('latinApp.converter module', function() {

  beforeEach(module('latinApp.converter'));

  describe('converter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var converterCtrl = $controller('converterCtrl');
      expect(converterCtrl).toBeDefined();
    }));

  });
});
