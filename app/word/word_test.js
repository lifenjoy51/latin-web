'use strict';

describe('latinApp.word module', function() {

  beforeEach(module('latinApp.word'));

  describe('word controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var wordCtrl = $controller('wordCtrl');
      expect(wordCtrl).toBeDefined();
    }));

  });
});
