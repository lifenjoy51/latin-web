'use strict';

describe('latinApp.quiz module', function() {

  beforeEach(module('latinApp.quiz'));

  describe('quiz controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var quizCtrl = $controller('quizCtrl');
      expect(quizCtrl).toBeDefined();
    }));

  });
});
