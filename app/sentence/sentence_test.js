'use strict';

describe('latinApp.sentence module', function() {

  beforeEach(module('latinApp.sentence'));

  describe('sentence controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var sentenceCtrl = $controller('sentenceCtrl');
      expect(sentenceCtrl).toBeDefined();
    }));

  });
});
